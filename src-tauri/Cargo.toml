[package]
name = "app"
version = "0.1.0"
description = "Accord — E2EE notes + chat, local-first"
authors = ["Jarrod Robinson"]
license = "AGPL-3.0-only"
repository = "https://github.com/jtrobinson1993/notes"
edition = "2021"
rust-version = "1.77.2"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.6.3", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
tauri = { version = "2.11.3", features = [] }
tauri-plugin-log = "2"
rusqlite = { version = "0.37", features = ["bundled-sqlcipher-vendored-openssl"] }
argon2 = "0.5"
zeroize = { version = "1", features = ["derive"] }
rand = "0.9"
thiserror = "2"
keyring = { version = "3", features = ["apple-native", "windows-native", "sync-secret-service"] }
aes-gcm = "0.10"
hkdf = "0.12"
sha2 = "0.10"
data-encoding = "2"
ed25519-dalek = "2"
x25519-dalek = { version = "2", features = ["static_secrets", "getrandom"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "stream"] }
base64 = "0.22"
tokio = { version = "1", features = ["rt", "macros", "time", "net"] }
tokio-tungstenite = { version = "0.24", features = ["rustls-tls-webpki-roots"] }
futures-util = "0.3"
# v8 key-transparency client verify (akd inclusion + key-history proofs). Only
# the light verify core — the full `akd` crate (storage/tree/audit) stays out of
# the app; consistency/append-only verification is the reference auditor's job.
akd_core = { version = "0.12", default-features = false, features = ["whatsapp_v1", "vrf", "serde_serialization"] }

[dev-dependencies]
tempfile = "3"
# The full akd crate, tests only — generates real proofs to verify against.
akd = { version = "0.12", features = ["whatsapp_v1"] }
